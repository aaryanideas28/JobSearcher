At a high level, JobSearcher acts as a brain-and-body system. The Agent folder holds the 'brain' (LangGraph logic), which coordinates what to do next. The Utils folder serves as the 'body,' handling the heavy lifting of talking to our PostgreSQL database and Redis cache. We use FastAPI as a 'monitor' to keep an eye on everything, making sure the agent is healthy and running smoothly